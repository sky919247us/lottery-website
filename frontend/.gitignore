# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# .env.production 只含公開的 API 位址，必須進版控才能到伺服器。
# 伺服器上沒有其他 .env，少了它 vite build 會 fallback 到 localhost:8000。
# 注意：此檔已進版控，不可寫入任何機密。
!.env.production
