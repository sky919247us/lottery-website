# 正式建置用的 API 位址。
# 伺服器上沒有 .env，若不固定在此檔，vite build 會 fallback 到
# src/hooks/api.ts 的 http://localhost:8000，導致整站資料載不到。
# 開發時 npm run dev 走 development mode，仍讀 .env.local，不受影響。
VITE_API_BASE=https://api.i168.win
